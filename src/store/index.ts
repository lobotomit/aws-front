import Vue from 'vue'
import Vuex from 'vuex'
import {profile} from "@/store/data";

Vue.use(Vuex);

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    profile
  }
})
